import Hero from '../components/Hero';

const Home = () => {
    return (
        <section className='min-h-[calc(100vh-117px)] flex justify-center items-center flex-col'>
            <Hero />
        </section>
    );
};

export default Home;
